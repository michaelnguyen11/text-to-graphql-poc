services:
  app:
    build: .
    ports:
      - "4444:4444"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./chroma_data:/app/chroma_data
    environment:
      - CHROMA_PERSIST_DIR=/app/chroma_data
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  chroma_data:
